# {{project_name}}

Load the `open-skills:harness-flow` skill before producing or modifying any deliverable — code, docs, or configuration.

## Repo Structure

<!-- COMMENT MUST DELETE: tree CLI format, using ├──/└── connectors; include key file names (manifests, primary entry files), not directories only. Add an inline # comment only on entries an agent would get wrong without being told. -->

```tree
{{repo_structure_tree}}
```

## Development Environment

<!-- COMMENT MUST DELETE: toolchain (language versions, package managers), external services, and env-var shapes (names, purpose, how to obtain) only — install and run/start commands belong to Workflow rows, not here. Never write a secret value here. -->

- Toolchain: {{language_versions_and_package_managers}}
- External services and environment variables: {{external_services_and_env_var_shapes_names_purpose_how_to_obtain}}

## Version Control

<!-- COMMENT MUST DELETE: one sentence by default, naming the user's VCS model and conventions (e.g. "This project uses trunk-based development with commitizen-formatted commit messages."). Write a detailed paragraph only when the user volunteered a concrete process. If the user declared a named VCS-strategy skill, the sentence instead reads: "This project uses the <skill> skill (<plugin> plugin). Load that skill before performing any <VCS> operation." If the user both named a skill and described a process, keep the skill sentence and add the process as a following paragraph. -->

{{vcs_declaration}}

## Workflow

<!-- COMMENT MUST DELETE: one row per user-declared phase, in the user's declared order (e.g. edit→verify; plan→build→verify; spec→plan→build→verify→release) — phase count, naming, and order are the user's, not mandated. Every command must be copy-paste runnable and have been executed (or dry-run) before being written here; manual steps are recorded as prose. -->

| Phase | How |
|---|---|
| {{phase_name}} | {{phase_how_command_skill_or_manual_step}} |

## Harness

<!-- COMMENT MUST DELETE: bullet list only — version bullet, four .harness/ file links each with a one-line applicability note, a short-term memory bullet (work-unit state file location, default .project/work-units/), a work-unit tool bullet (declared tool, or none), a session memory bullet (handoff file location, default .project/handoff.md), and the merge-moment write-discipline note pointing at the harness-flow skill. -->

- open-skills plugin version: v{{open_skills_plugin_version}}
- `.harness/ARCHITECTURE.md` — module map, layering, key boundaries; read before making a structural change.
- `.harness/CONSTRAINTS.md` — hard MUST / MUST NOT rules with source and applicability; read before any change that could violate one.
- `.harness/DECISIONS.md` — index of active decisions (details in `.harness/decisions/`); read before revisiting a past call.
- `.harness/FEATURES.md` — passing-only index of verified behavior (details in `.harness/features/`); read to see what already works and how it was verified.
- short-term memory: {{work_unit_state_file_location_default_dot_project_work_units}}
- work-unit tool: {{declared_tool_that_carries_in_flight_change_state_or_none}}
- session memory: {{handoff_file_location_default_dot_project_handoff_md}}
- `.harness/` is read-anytime and written only by the orchestrator at merge moments — see the harness-flow skill (or run `/open-skills:harness-flow`) for the loop that enforces this.
